server:
  port: 8100
spring:
  datasource:
    username: root
    password: Passw0rd2
    url: jdbc:mysql://localhost:3306/reservation_service?useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      javax:
        persistence:
          validation:
            mode: none
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
  application:
    name: RESERVATION-SERVICE

